{
  "name": "thergrid",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test",
    "coverage": "node --test --experimental-test-coverage",
    "syntax": "find src tests scripts -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "demo": "node scripts/demo.mjs",
    "check": "npm run syntax && npm test && npm run demo"
  }
}
