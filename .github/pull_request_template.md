## Objective
Describe the smallest THERGRID boundary or capability this PR changes.

## Scope
- What changed:
- What was intentionally left unchanged:
- Related issue/PR:

## Evidence / authority boundary
- Evidence producer:
- Snapshot / experiment / request identity affected:
- Provenance or solver fields trusted:
- Stale, substituted, or malformed inputs rejected:
- Authority explicitly **not** added by this PR:

## Validation
- [ ] Reproducible install and dependency audit pass.
- [ ] Syntax/lint/format gates pass.
- [ ] Focused tests cover the changed behavior and unsafe path.
- [ ] Coverage remains above the enforced floor.
- [ ] Demo/evidence path remains deterministic when affected.
- [ ] Container verification remains green.
- [ ] CodeQL/security gate passes on the exact head.
- [ ] No physical actuation, automatic promotion, or provider-specific authority was introduced.
- [ ] No credentials, live customer/grid data, or control secrets were added.

## Reviewer attack surface
List the stale telemetry, snapshot swap, cross-request evidence reuse, solver timeout/infeasibility, accessor/symbol/prototype tricks, renderer target substitution, or authority-smuggling cases reviewers should try.

## Merge note
If stacked, state merge order and retest plan. Otherwise write `not stacked`.
