# Optimization measurement receipt

This receipt records the versioned problem shape, solver configuration, backend identity, algorithm identity, objective values, comparison evidence, and elapsed runtime for a bounded optimization measurement.

It is an observational record. It does not modify telemetry or application state. Future quantum and quantum-inspired implementations can emit the same evidence shape while remaining comparable with the classical reference.
